 name: "azure-kubernetes-cluster-workflow"


 on: 
   push:
     branches:
       - main

 jobs:
   terraform-job:
     runs-on: ubuntu-latest
     
     steps:
      - name: Checkout
        uses: actions/checkout@v5.1.0
        
